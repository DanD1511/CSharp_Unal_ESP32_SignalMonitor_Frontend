<Window x:Class="CSharp_WPF_Websockets.Presentation.Views.DetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:CSharp_WPF_Websockets.Presentation.Views"
        xmlns:converters="clr-namespace:CSharp_WPF_Websockets.Presentation.Converters"
        Title="Signal Detail View" 
        Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#FF1A1A1A"
        FontFamily="Segoe UI"
        FontWeight="Light">

    <Window.Resources>
        <SolidColorBrush x:Key="DarkBackground" Color="#FF1A1A1A"/>
        <SolidColorBrush x:Key="DarkCard" Color="#FF2A2A2A"/>
        <SolidColorBrush x:Key="DarkBorder" Color="#FF404040"/>
        <SolidColorBrush x:Key="AccentColor" Color="#FF0078D4"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFE1E1E1"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FF9A9A9A"/>
        <SolidColorBrush x:Key="SelectedColor" Color="#FF4ECDC4"/>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <!--<converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>-->

        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.4"/>
        </Storyboard>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3A3A3A"/>
                                <Setter Property="BorderBrush" Value="{StaticResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource DarkCard}" 
                BorderBrush="{StaticResource DarkBorder}" BorderThickness="0,0,0,1">
            <Grid Margin="30,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📊" FontSize="20" Margin="0,0,12,0"/>
                    <TextBlock Text="Signal Detail View" 
                             Foreground="{StaticResource TextPrimary}"
                             FontSize="18" FontWeight="Light"/>
                    <TextBlock Text="{Binding SelectedSignals.Count, StringFormat=' — {0} signals'}" 
                             Foreground="{StaticResource SelectedColor}"
                             FontSize="14" 
                             VerticalAlignment="Center"
                             Margin="8,0,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" 
                       Style="{StaticResource CloseButton}"
                       Click="CloseButton_Click">
                    <TextBlock Text="✕ Close" FontSize="12"/>
                </Button>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto" 
                     Margin="20"
                     Visibility="{Binding HasSelectedSignals, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding SelectedSignals}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource DarkCard}"
                                BorderBrush="{StaticResource DarkBorder}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="0,0,0,20"
                                Padding="30">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="400"/>
                                </Grid.RowDefinitions>

                                <!-- Signal Header -->
                                <Grid Grid.Row="0" Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding SignalName}" 
                                                 Foreground="{StaticResource TextPrimary}"
                                                 FontSize="24" 
                                                 FontWeight="Normal"
                                                 Margin="0,0,0,8"/>

                                        <StackPanel Orientation="Horizontal">
                                            <Border Background="{Binding SignalColor}"
                                                  Width="20" Height="20"
                                                  CornerRadius="3"
                                                  Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding SignalType}" 
                                                     Foreground="{StaticResource TextSecondary}"
                                                     FontSize="12"
                                                     VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding CurrentValue, StringFormat='{}{0:F2}'}" 
                                                 Foreground="{StaticResource SelectedColor}"
                                                 FontSize="36" 
                                                 FontWeight="Light"
                                                 HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding SignalUnit}" 
                                                 Foreground="{StaticResource TextSecondary}"
                                                 FontSize="14"
                                                 HorizontalAlignment="Right"
                                                 Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Chart -->
                                <views:SignalCard Grid.Row="1" DataContext="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <Border Grid.Row="1" 
                Background="{StaticResource DarkBackground}"
                Visibility="{Binding HasSelectedSignals}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="📊" 
                         FontSize="72" 
                         Foreground="{StaticResource DarkBorder}"
                         HorizontalAlignment="Center"
                         Margin="0,0,0,20"/>
                <TextBlock Text="No signals selected" 
                         Foreground="{StaticResource TextSecondary}"
                         FontSize="20" 
                         FontWeight="Light"
                         HorizontalAlignment="Center"
                         Margin="0,0,0,8"/>
                <TextBlock Text="Select up to 3 signals from the main window" 
                         Foreground="{StaticResource TextSecondary}"
                         FontSize="14"
                         HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>
</Window>