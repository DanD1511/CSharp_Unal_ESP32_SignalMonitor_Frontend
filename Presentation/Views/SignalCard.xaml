<UserControl x:Class="CSharp_WPF_Websockets.Presentation.Views.SignalCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:converters="clr-namespace:CSharp_WPF_Websockets.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <SolidColorBrush x:Key="TextPrimary" Color="#2D3436"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#636E72"/>
        <SolidColorBrush x:Key="TextMuted" Color="#B2BEC3"/>
        <SolidColorBrush x:Key="AccentColor" Color="#0984E3"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="BgHover" Color="#F1F5F9"/>

        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="{StaticResource BgHover}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="bdr" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AutoToggle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="bdr" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TinyInput" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="12,10,12,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                <TextBlock Text="{Binding DisplayValue}" 
                           FontSize="26" 
                           FontWeight="Light" 
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,0,4,0"/>
                <TextBlock Text="{Binding Unit}" 
                           FontSize="14" 
                           Foreground="{StaticResource TextMuted}" 
                           VerticalAlignment="Bottom" 
                           Margin="0,0,0,5"/>
            </StackPanel>

            <TextBlock Grid.Column="1" 
                       Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss.fff}'}" 
                       Foreground="{StaticResource TextMuted}" 
                       FontSize="11" 
                       VerticalAlignment="Bottom" 
                       Margin="0,0,0,6"/>
        </Grid>

        <Grid Grid.Row="1" Margin="12,0,12,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Background="#FAFAFA" CornerRadius="3" Padding="6,2" BorderThickness="1" BorderBrush="{StaticResource BorderColor}" HorizontalAlignment="Left">
                <TextBlock FontSize="10" Foreground="{StaticResource TextSecondary}">
                    <Run Text="Y:" FontWeight="Bold"/>
                    <Run Text="{Binding YMin, StringFormat='{}{0:F1}'}"/>
                    <Run Text=" to "/>
                    <Run Text="{Binding YMax, StringFormat='{}{0:F1}'}"/>
                    <Run Text=" | T:" FontWeight="Bold"/>
                    <Run Text="{Binding TimeWindowMinutes, StringFormat='{}{0:F2}m'}"/>
                </TextBlock>
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ToggleButton Style="{StaticResource AutoToggle}" 
                              Content="AUTO" 
                              IsChecked="{Binding IsAutoScale}" 
                              Command="{Binding ToggleAutoScaleCommand}"
                              ToolTip="Auto Scale Y-Axis"/>

                <Button Style="{StaticResource ToolButton}" Command="{Binding ZoomInCommand}" ToolTip="Zoom In (+)">
                    <TextBlock Text="+" FontWeight="Bold" Margin="0,-2,0,0"/>
                </Button>

                <Button Style="{StaticResource ToolButton}" Command="{Binding ZoomOutCommand}" ToolTip="Zoom Out (-)">
                    <TextBlock Text="-" FontWeight="Bold" Margin="0,-2,0,0"/>
                </Button>

                <Rectangle Width="1" Fill="{StaticResource BorderColor}" Margin="4,3"/>

                <Button Style="{StaticResource ToolButton}" Command="{Binding ToggleScaleControlsCommand}" ToolTip="Settings">
                    <TextBlock Text="⚙" FontSize="14"/>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2" Background="#f3f5f9" Margin="5,0,5,5">
            <lvc:CartesianChart x:Name="SignalChart"
                                Series="{Binding ChartSeries, UpdateSourceTrigger=PropertyChanged}"
                                XAxes="{Binding XAxes, UpdateSourceTrigger=PropertyChanged}"
                                YAxes="{Binding YAxes, UpdateSourceTrigger=PropertyChanged}"
                                AnimationsSpeed="00:00:00"
                                EasingFunction="{x:Null}"
                                TooltipPosition="Hidden"/>
        </Grid>

        <Border Grid.Row="3" MinHeight="100"
                Background="#FAFAFA" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="0,1,0,0" 
                Padding="12"
                Visibility="Visible">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Min:" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,0,5,0"/>
                    <TextBox Grid.Column="1" Text="{Binding ManualYMin}" Style="{StaticResource TinyInput}"/>

                    <TextBlock Grid.Column="3" Text="Max:" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,0,5,0"/>
                    <TextBox Grid.Column="4" Text="{Binding ManualYMax}" Style="{StaticResource TinyInput}"/>

                    <TextBlock Grid.Column="6" Text="Min (T):" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,0,5,0"/>
                    <TextBox Grid.Column="7" Text="{Binding ManualTimeWindow}" Style="{StaticResource TinyInput}"/>
                </Grid>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Reset Default" Command="{Binding ResetScaleCommand}" Style="{StaticResource ToolButton}" Width="Auto" Padding="8,0" Margin="0,0,8,0"/>
                    <Button Content="Apply" Command="{Binding ApplyManualScaleCommand}" Style="{StaticResource ToolButton}" Width="Auto" Padding="12,0" Background="{StaticResource AccentColor}" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>